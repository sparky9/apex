<Window x:Class="ApexV2.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input Required" 
        Height="160" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.WindowBackground}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Name="PromptText" Text="Enter value:" 
                   Foreground="{DynamicResource Brush.TextPrimary}"/>
        
        <TextBox Grid.Row="2" Name="InputTextBox" Height="25" 
                 Padding="5,2" 
                 Background="{DynamicResource Brush.InputBackground}"
                 Foreground="{DynamicResource Brush.TextPrimary}"
                 BorderBrush="{DynamicResource Brush.BorderBrush}"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="OkButton" Content="OK" Width="75" Height="25" 
                    Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Name="CancelButton" Content="Cancel" Width="75" Height="25" 
                    IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
