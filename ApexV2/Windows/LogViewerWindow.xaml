<Window x:Class="ApexV2.Windows.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Viewer" Height="500" Width="900" WindowStartupLocation="CenterOwner"
        FontFamily="Consolas" Background="#1E1E1E" Foreground="#DDD">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0 0 0 6" Grid.Row="0">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0 0 6 0"/>
            <ComboBox x:Name="LevelFilter" Width="110" SelectedIndex="0" SelectionChanged="Refresh_Click">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Trace"/>
                <ComboBoxItem Content="Debug"/>
                <ComboBoxItem Content="Info"/>
                <ComboBoxItem Content="Warn"/>
                <ComboBoxItem Content="Error"/>
                <ComboBoxItem Content="Critical"/>
            </ComboBox>
            <TextBox x:Name="SearchBox" Width="200" Margin="8 0 0 0" TextChanged="SearchBox_TextChanged"/>
            <Button Content="Refresh" Margin="8 0 0 0" Click="Refresh_Click"/>
            <Button Content="Copy Selected" Margin="8 0 0 0" Click="CopySelected_Click"/>
            <Button Content="Export Visible" Margin="8 0 0 0" Click="ExportVisible_Click"/>
            <Button Content="Close" Margin="8 0 0 0" Click="Close_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Newest at bottom" FontSize="11" Foreground="#888" Margin="2 0 0 4"/>
        <ListView x:Name="LogList" Grid.Row="2" BorderBrush="#333" BorderThickness="1" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridViewColumn Header="Time" Width="170" DisplayMemberBinding="{Binding TimestampUtc}"/>
                    <GridViewColumn Header="Level" Width="70" DisplayMemberBinding="{Binding Level}"/>
                    <GridViewColumn Header="Category" Width="140" DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="Message" Width="400" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
