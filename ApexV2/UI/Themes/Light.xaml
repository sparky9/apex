<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Color x:Key="Color.Background.Window">#F2F4F6</Color>
  <Color x:Key="Color.Background.Panel">#FFFFFF</Color>
  <Color x:Key="Color.Background.Input">#FFFFFF</Color>
  <Color x:Key="Color.Border">#D0D5DA</Color>
  <Color x:Key="Color.Accent">#2566CC</Color>
  <Color x:Key="Color.Text.Primary">#1E1E1E</Color>
  <Color x:Key="Color.Text.Secondary">#555A60</Color>
  <Color x:Key="Color.Danger">#D93030</Color>
  <Color x:Key="Color.Warning">#E6A700</Color>
  <Color x:Key="Color.Success">#1F8D4D</Color>

  <SolidColorBrush x:Key="Brush.WindowBackground" Color="{StaticResource Color.Background.Window}"/>
  <SolidColorBrush x:Key="Brush.PanelBackground" Color="{StaticResource Color.Background.Panel}"/>
  <SolidColorBrush x:Key="Brush.InputBackground" Color="{StaticResource Color.Background.Input}"/>
  <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Color.Border}"/>
  <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Accent}"/>
  <SolidColorBrush x:Key="Brush.TextPrimary" Color="{StaticResource Color.Text.Primary}"/>
  <SolidColorBrush x:Key="Brush.TextSecondary" Color="{StaticResource Color.Text.Secondary}"/>
  <SolidColorBrush x:Key="Brush.Danger" Color="{StaticResource Color.Danger}"/>
  <SolidColorBrush x:Key="Brush.Warning" Color="{StaticResource Color.Warning}"/>
  <SolidColorBrush x:Key="Brush.Success" Color="{StaticResource Color.Success}"/>

  <Style x:Key="Text.Primary" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource Brush.TextPrimary}"/>
  </Style>
  <Style x:Key="Text.Secondary" TargetType="TextBlock" BasedOn="{StaticResource Text.Primary}">
    <Setter Property="Foreground" Value="{DynamicResource Brush.TextSecondary}"/>
  </Style>

  <Style TargetType="Button" x:Key="Button.Primary">
    <Setter Property="Background" Value="{DynamicResource Brush.Accent}"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="10,4"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#2E79E6"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#1C4F91"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
