<UserControl x:Class="ApexV2.Dashboard.DashboardPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="10">
            <TextBlock Text="APEX Dashboard" Foreground="White" FontSize="18" FontWeight="Bold"/>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Portfolio -->
            <Border Grid.Column="0" Background="#F8F9FA" Margin="0,0,5,0" CornerRadius="5">
                <StackPanel Margin="15">
                    <TextBlock Text="Portfolio Overview" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ContentPresenter x:Name="PortfolioContent"/>
                </StackPanel>
            </Border>
            
            <!-- Right Panel - Market Data -->
            <Border Grid.Column="1" Background="#F8F9FA" Margin="5,0,0,0" CornerRadius="5">
                <StackPanel Margin="15">
                    <TextBlock Text="Market Overview" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ContentPresenter x:Name="MarketContent"/>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#34495E" Padding="10">
            <TextBlock x:Name="StatusText" Text="Dashboard Ready" Foreground="White"/>
        </Border>
    </Grid>
</UserControl>
